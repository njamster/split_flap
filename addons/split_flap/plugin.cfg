[plugin]

name="SplitFlap"
description="A Control node that emulates a split-flap display. It steps through a predefined sequence of characters until it reaches the target. Each character is split into two halves and when the current character changes, the upper half will drop down."
author="njamster"
version="1.0"
script="plugin.gd"
