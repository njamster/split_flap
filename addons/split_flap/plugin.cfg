[plugin]

name="SplitFlap"
description="A control that emulates a split-flap display. It steps through a fixed array of characters (predefined by the user) until it reaches the target character. Each character gets vertically split into two halves, and when the current character changes, the upper half will drop down."
author="njamster"
version="1.0"
script="plugin.gd"
